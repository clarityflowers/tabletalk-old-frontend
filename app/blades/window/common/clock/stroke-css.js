import { css } from 'styled-components';
import Colors from 'blades/common/colors';

const { sun } = Colors;

const stroke = css`
  stroke: ${sun};
  fill: none;
  pointer-events: none;
`

export default stroke;
